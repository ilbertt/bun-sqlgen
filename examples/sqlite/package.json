{
  "name": "@repo/example-sqlite",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun codegen && bun build src/index.ts --outdir dist --target bun",
    "codegen": "bun-sqlgen generate 'src/**/*.ts' --migrations src/db/migrations --package @repo/bun-sqlgen --dialect sqlite",
    "codegen:check": "bun-sqlgen generate 'src/**/*.ts' --migrations src/db/migrations --package @repo/bun-sqlgen --dialect sqlite --check",
    "check:types": "bun codegen:check && bun run --bun tsc"
  },
  "devDependencies": {
    "@repo/bun-sqlgen": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@types/bun": "catalog:",
    "typescript": "catalog:"
  },
  "imports": {
    "#*": "./src/*"
  }
}
